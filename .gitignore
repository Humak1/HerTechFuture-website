# dependencies
/node_modules
/.pnp
.pnp.*

# next.js build output
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# environment files -- NEVER commit these.
# If a secret ever lands in a commit, rotate the secret; deleting the commit
# is not enough, because the value is still in the repository history.
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
